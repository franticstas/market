interface Product {
    id : string;
    name: string;
    price: number;
}

interface ProductCard {
    name: string;
    description: string;
    category: string;
    images: string;
}

export {Product, ProductCard}